#!/usr/bin/env bash

readonly BIN="/usr/bin/memcached"
readonly NICE="/bin/nice"
readonly NICENESS="0"
readonly OPTIONS="
 -u memcached
 -p 11211
 -m ${MEMCACHED_CACHESIZE:-64}
 -c ${MEMCACHED_MAXCONN:-1024}
 ${MEMCACHED_OPTIONS:-}
"

exec ${NICE} \
	-n ${NICENESS} \
	${BIN} \
	${OPTIONS}
